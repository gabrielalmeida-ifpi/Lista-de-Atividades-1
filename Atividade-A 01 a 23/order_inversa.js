/**
 * Leia 2 números (A, B) e escreva-os em ordem inversa (B, A).
 */

const prompt = require('prompt-sync')();

//Entrada
const numeroA = Number(prompt('Insira o número A: '))
const numeroB = Number(prompt('Insira o número B: '))

//Processamento

//Não é necessário processamento!

//Saída
console.log('A ordem invertida desses números é: ','(',numeroB,',',numeroA,')')
